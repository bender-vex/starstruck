
#include "lib/joystick.h";

task main() {

    // Calibrate the sensors and robot

    // Main control loop
    while (true) {

        // Poll joysticks and sensors

        // Run raw data through PID controller, this ensures all input is sanitized

        // Pass sanitized input to robot
    }
}
